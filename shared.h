#include <vector>
#include <string>
#include <iostream>
#include <chrono>
#include <cstdlib>
#include <cstring>

typedef long long integer;
typedef long double rational;

namespace shared {
	integer pow(const integer a , const integer b);
	rational ator(const std::string&);
	
}
